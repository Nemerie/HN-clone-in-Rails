<tr>
  <td>
    <table class="fatitem" border="0">
      <tbody>
        <tr class="athing">
          <td class="ind"></td>
          <td valign="top" class="votelinks">
            <center>
              <%= link_to content_tag(:div, '', class: "votearrow", title: "upvote"),
                          "/comments/#{@comment.id}/upvote", method: :post %>
            </center>
          </td>
          <td class="default">
            <div style="margin-top:2px; margin-bottom:-10px;">
              <span class="comhead">
                <%= pluralize(@comment.votes_count, 'point') %> by
                <%= link_to @comment.user.name, "/users/#{@comment.user.id}", class: "hnuser" %>
                <span class="age">
                  <%= link_to time_ago_in_words(@comment.created_at) + " ago", "/comments/#{@comment.id}" %>
                </span>
                <span class="storyon"> on:
                  <%= link_to @comment.post.title, "/posts/#{@comment.post.id}" %>
                </span>
              </span>
            </div>
            <br>
            <div class="comment">
              <span class="commtext c00">
                <%= @comment.content %>
                <div class="reply"></div>
              </span>
            </div>
          </td>
        </tr>
        <tr style="height:10px"></tr>
        <tr>
          <td colspan="2"></td>
          <td>
            <%= form_with(url: '/comments/new', local: true) do |f| %>
              <%= hidden_field_tag "post", @comment.post.id %>
              <%= hidden_field_tag "parent", @comment.id %>
              <%= f.text_area :content, rows: "6", cols: "60" %>
              <br><br>
              <%= f.submit "submit" %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
    <br><br>
  </td>
</tr>
